# Provider plugins/state — never commit.
**/.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Per-deployer overrides — commit terraform.tfvars.example only.
terraform.tfvars
*.auto.tfvars

override.tf
override.tf.json
*_override.tf
*_override.tf.json
